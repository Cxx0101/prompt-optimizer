interface:
  display_name: "Image Prompt Optimizer"
  short_description: "Compile rough visual ideas into model-ready image prompts"
  default_prompt: "Optimize this image idea into a production-ready prompt while preserving my intent."
policy:
  allow_implicit_invocation: true
