interface:
  display_name: "Prompt Optimizer"
  short_description: "Analyze references into image and video prompts"
  default_prompt: "Use $prompt-optimizer to turn my reference image, video, or brief into a model-ready prompt."
policy:
  allow_implicit_invocation: true
