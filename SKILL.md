---
name: prompt-optimizer
description: "分析参考图、参考视频、脚本、文案或视觉 brief，并转换为可执行的生图或生视频提示词；适用于 GPT Image、Seedream 5.0 与 Seedance 2.0 的生成、编辑、风格迁移、参考复刻和多素材组合。"
license: "Private"
metadata:
  hermes:
    tags: [image-generation, video-generation, multimodal-reference, prompting]
    category: creative
---

# Prompt Optimizer

把参考图、参考视频、文本内容或粗略创意转换成可直接使用的图像 / 视频生成提示词。

默认只分析素材并输出 Prompt，不直接调用生成工具；用户明确要求生成或编辑媒体时，才执行对应操作。

## 支持范围

- GPT Image：文生图、参考图生图、图片编辑、多图合成
- Seedream 5.0：文生图、参考图生图、图片编辑、组图
- Seedance 2.0：文生视频、图生视频、参考视频生视频、多模态参考、视频编辑与延长
- 跨模态转换：图转视频提示词、视频转关键画面提示词、脚本 / 文案转画面或分镜提示词

不要输出这三种目标模型之外的专用语法。

## 先确定交付目标

识别：

1. 输出是图片还是视频；
2. 是新生成、编辑、复刻、风格迁移、主体替换、延长还是多素材组合；
3. 目标模型；
4. 参考素材各自承担什么角色；
5. 哪些内容必须保留，哪些内容允许改变。

模型未指定时：

- 用户要生图：分别输出 GPT Image 与 Seedream 5.0 两版；
- 用户要生视频：输出 Seedance 2.0 版；
- 交付物确实无法判断是图还是视频时，只追问这一项。

## 素材可用性

- 已附带图片：直接观察，不要求用户重复描述可见内容。
- 已附带视频：检查完整时序；至少覆盖开头、动作变化、镜头变化和结尾，不能只看封面帧。
- 提供多份素材：按出现顺序编号为“图 1 / 视频 1 / 音频 1 / 文本 1”。
- 任务依赖具体素材但当前不可读取：请用户重新上传或粘贴内容。
- 不把文件名、用户概述或单帧缩略图冒充完整素材分析。

分析参考素材时读取 `references/reference-analysis.md`。

## 参考分析核心

在内部建立一份 Reference Map，不默认向用户展示分析过程。

### 1. Source Role

为每份素材分配一个或多个明确角色：

- 主体 / 身份
- 产品 / 道具
- 场景 / 空间
- 构图 / 机位
- 风格 / 材质 / 色彩
- 动作 / 表演
- 运镜 / 节奏 / 转场
- 声音 / 台词 / 音乐
- 文案 / 故事 / 信息结构

不要笼统写“参考全部”。同一特征只能有一个优先来源；多素材冲突时先解决归属。

### 2. Anchors

提取必须稳定保留的锚点，例如：

- 人物的可见外观、发型、服装轮廓和身体比例
- 产品轮廓、比例、材质、Logo 与包装文字
- 空间布局、相机高度、主体位置和前后景关系
- 视频中的动作顺序、节奏节点、镜头轨迹和声音事件

只记录可观察事实。不要无依据推断人物真实身份、镜头精确焦距、品牌信息或制作参数。

### 3. Creative Delta

把用户要求转换成明确变化：

- 改哪个对象或时段；
- 从什么变成什么；
- 变化幅度；
- 新内容如何与原素材融合；
- 哪些区域、镜头或声音保持不变。

### 4. Priority

冲突时按以下顺序处理：

1. 用户本轮明确要求；
2. 用户指定的素材角色；
3. 多份素材中一致、清晰的可见证据；
4. 合理且克制的视觉默认值。

不要为了“更丰富”加入参考素材中没有、用户也没要求的角色、道具、文字、镜头或剧情。

## 跨模态转换

### 参考图 → 生图

锁定被指定的主体、产品、构图或风格，补充目标画面的静态关系。局部编辑必须明确 Change / Preserve / Integration / Constraints。

### 参考图 → 生视频

参考图提供外观与起始空间，不会自动提供合理动作。必须补充：

- 初始状态；
- 动作的起因、过程和结束状态；
- 镜头是否移动；
- 环境中会响应动作的元素；
- 声音是否需要生成。

不要让主体在第一帧无缘无故跳变，也不要同时要求互相冲突的动作和运镜。

### 参考视频 → 生图

选择最能表达目标的决定性瞬间，把时间信息转换成静态可见线索，例如姿势、衣物惯性、运动模糊、飞溅物、视线与构图张力。不要把整段镜头动作原样塞进单张图片 Prompt。

### 参考视频 → 生视频

先区分要参考的是主体、动作、运镜、节奏、场景、风格还是声音。除非用户要求复刻，不要默认复制原视频的全部内容。

### 文本 / 脚本 / 文案 → 图或视频

先提取不可改动的事实、事件、文案和顺序，再把抽象词转换成可见或可听的表现。单张图选择一个清晰瞬间；视频按时间组织事件，不能把整篇内容平均塞进数秒视频。

## 生图工作流

1. 确定图片用途和画幅方向。
2. 建立主体、行为、环境和空间关系。
3. 选择一个清晰构图、机位和光线逻辑。
4. 把参考锚点与 Creative Delta 合并。
5. 添加必要的材质、色彩、文字和保留约束。
6. 编译到目标模型，不虚构界面参数。

需要视觉描述词时读取 `references/visual-language.md`。

- GPT Image：读取 `references/model-gpt-image.md`。
- Seedream 5.0：读取 `references/model-seedream-5.md`。

## 生视频工作流

1. 确定时长、画幅、用途和单镜头 / 多镜头结构。
2. 建立开头状态、事件变化和结尾状态。
3. 为动作写清主体、方向、速度、作用对象和物理反馈。
4. 只选择能服务叙事的一套运镜；主体运动和相机运动分开描述。
5. 为多镜头分配时间，明确景别、动作、运镜和转场。
6. 声音按环境音、动作音、台词、音乐分别描述；不需要时明确无音乐或无对白。
7. 写出人物 / 产品 / 场景连续性与禁止漂移项。

Seedance 2.0 任务读取 `references/model-seedance-2.md`。

## 编辑类任务的固定逻辑

任何图片或视频编辑都必须在内部覆盖：

- **Change**：只改什么对象、区域、片段或声音；
- **Preserve**：身份、产品、构图、时间线中哪些内容保持；
- **Integration**：透视、比例、光线、阴影、反射、动作惯性、焦点、颗粒和声音空间如何融合；
- **Constraints**：不重构、不改写、不新增什么。

不要把局部编辑重写成整幅画面或整段视频的重新生成描述。

## Prompt 编译规则

- 使用清楚、连贯的自然语言，关键词列表只用于确有价值的短属性。
- 先写创作目标，再写主体与事件，再写空间 / 时间关系，最后写约束。
- 多素材必须显式引用编号并说明职责，例如“图 1 锁定人物，视频 1 仅参考运镜”。
- 抽象词必须落到可执行表现：“高级”要体现为留白、材质、高光控制和层级；“电影感”要体现为景别、机位、光线、运动和节奏。
- 不伪造无法从素材确认的颜色、文字、动作或镜头参数。
- 不机械堆叠“8K、masterpiece、best quality”等质量词。
- 不默认输出 Negative Prompt；把关键禁止项写成直接约束。
- 用户要求准确文字时，逐字引用文案，并注明位置、层级、字体方向和“不得出现其他文字”。

## 默认输出

### 单一目标模型

```text
目标模型
<GPT Image / Seedream 5.0 / Seedance 2.0>

提示词
<可直接复制的最终 Prompt>

素材分工
- <仅在有多份素材时输出>

关键保留
- <仅在参考或编辑任务中输出>

建议设置
- <仅输出用户界面中确定可用的时长、画幅、分辨率或质量建议>
```

### 生图但未指定模型

分别给出“GPT Image 版”和“Seedream 5.0 版”，不要简单复制同一段文字。

### 需要多镜头的视频

最终 Prompt 可以使用时间段或镜头编号，但总时长必须自洽。简单单镜头不要强行拆分。

用户只要 Prompt 时，不额外输出长篇分析、教程或使用步骤。

## 追问边界

只在以下情况追问一个最关键问题：

- 依赖具体参考素材但素材不可用；
- 无法判断交付物是图片还是视频；
- 替换图片文字但新文案缺失；
- 多份素材对同一角色存在关键冲突且无法根据用户意图消解。

普通镜头、光线、材质、节奏和环境细节可合理补全，不频繁追问。

## 输出前检查

读取 `references/evaluation.md` 做快速 QA。重点确认：

- 用户意图没有被参考素材或审美补全覆盖；
- 每份素材角色明确且没有冲突；
- 静态 Prompt 没有混入无意义时间线；
- 视频 Prompt 包含可拍摄的动作顺序和结尾；
- 保留项、变化项和融合要求没有互相矛盾；
- 模型名称、语法和能力与目标模型匹配。

需要成品示例时读取 `references/examples.md`。
